language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t chesterhyang/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true chesterhyang/react-test npm test

after_success:
  - docker build -t chesterhyang/multi-client ./client
  - docker build -t chesterhyang/multi-nginx ./nginx
  - docker build -t chesterhyang/multi-server ./server
  - docker build -t chesterhyang/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push chesterhyang/multi-client
  - docker push chesterhyang/multi-nginx
  - docker push chesterhyang/multi-server
  - docker push chesterhyang/multi-worker

deploy:
  region: us-east-1
  edge: true
  provider: elasticbeanstalk
  app: multi-docker
  env: MultiDocker-env-1
  bucket_name: elasticbeanstalk-us-east-1-768104751743
  bucket_path: docker-multi
  on:
    branch: master
  access_key_id: $AWS_ACCESS_KEY
  secret_access_key: $AWS_SECRET_KEY
